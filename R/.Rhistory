library(dplyr)
temp %>% mutate_at(vars(-cell_col),as.numeric)
temp = as.data.frame(cbind(c("a","b","c","d"),c(1:4)))
colnames(temp) = c("cell_id","exon")
cell_col = "cell_id"
temp %>% mutate_at(vars(-cell_col),as.numeric)
temp %>% mutate_at(vars(-cell_col),as.numeric)
